version: '3.7'

services:
  db:
    environment: 
       POSTGRES_PASSWORD: Mo%902ose
       POSTGRES_USER: mooseGre
       POSTGRES_DB: amort
#    restart: always
    image: postgres:10-alpine
#    expose:
#      - "5432"
#    ports:
#      - "5432:5432"
  web:
    build:  .
#    command: python /app/manage.py makemigrations amortization
#    command: python /app/manage.py migrate
    command: python /app/manage.py runserver 0.0.0.0:8000

    ports:
      - 8000:8000
    depends_on:
      - db
