# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Smart Temperature Controller UI' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Smart Temperature Controller UI
  	pod 'SwiftyJSON'
  	pod 'Alamofire'

  target 'Smart Temperature Controller UITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Smart Temperature Controller UIUITests' do
    inherit! :search_paths
    # Pods for testing
  end


end

